# # math_json_ops/setup.py

# from setuptools import setup, find_packages

# setup(
#     name='math_json_ops',
#     version='0.1',
#     packages=find_packages(),
#     include_package_data=True,
#     install_requires=[],
#     entry_points={
#         'console_scripts': [
#             'math-json-ops=math_json_ops.file_operations:main',
#         ],
#     },
# )
# setup.py

from setuptools import setup, find_packages

setup(
    name='math_json_ops',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
)
